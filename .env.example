NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Used by Monthly Review goal progress card (server-side).
MONTHLY_GOAL_USD=5000

# Force mock trades/screenshots even if Supabase env vars exist (great for UI demos).
# TRADING_JOURNAL_USE_MOCK=true
